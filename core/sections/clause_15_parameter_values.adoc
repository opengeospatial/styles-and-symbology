== Symbolizer Parameter Value Expresssions
=== Overview

This conformance class adds support for using **ParameterValues** i.e., arbitrary **Expressions** (not only literals), for all **Symbolizer** properties.
